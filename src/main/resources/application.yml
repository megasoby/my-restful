server:
  port: 8088

spring:
  output:
    ansi.enabled: always
  messages:
    basename: messages
    fallback-to-system-locale: false
#  security:
#    user:
#      name: megasoby
#      password: passw0rd
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
  h2:
    console:
      enable: true

logging:
  level:
    org.springframework: DEBUG





